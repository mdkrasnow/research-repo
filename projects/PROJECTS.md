# Projects

- `sample-toy-experiment` : minimal end-to-end demo (local + SLURM-ready)

To add a project:
1. Copy `templates/project` -> `projects/<slug>`
2. Update this file
3. (Optional) run `/project-init --project <slug>`
