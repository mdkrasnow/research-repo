A run has failed. Choose the correct intervention lever from the trace below.

{trace_text}

Optimize the HIDDEN measured outcome (regret against the best real intervention), not just clean
prediction error. Remember: training weights against a broken harness makes things worse.

Respond with strict JSON only: {{"action": "...", "reason": "..."}}
