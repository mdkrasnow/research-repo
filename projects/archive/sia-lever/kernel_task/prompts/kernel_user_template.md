Write `kernel(a, b)` for the op above.

Deployed verifier: {verifier_desc}
Target device: {device}. Baseline (torch) latency to beat: {baseline_ms} ms.

{feedback}

Return ONLY the python code block defining kernel(a, b).
