__pycache__/
*.pyc
.DS_Store

# vendored official SIA repo — reproducible via baselines/vendor/sia_commit.txt (do not commit)
baselines/vendor/sia/
# large LoRA adapter checkpoints (keep provenance .json/.txt only if desired)
adapters/gpt_oss_120b/*/
# transient rollout/test jsonl
results/gpt_oss/*_rollouts_*.jsonl
