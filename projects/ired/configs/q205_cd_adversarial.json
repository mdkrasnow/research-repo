{
  "experiment_name": "q205_cd_adversarial",
  "description": "IRED-CD Investigation - Stage 4: CD loss with adversarial (opt_step) negatives, no Langevin",
  "investigation": "ired-cd",
  "ablation_stage": "cd_adversarial",
  "mining_strategy": "adversarial",
  "rank": 20,
  "ood": false,
  "diffusion_steps": 10,
  "batch_size": 2048,
  "train_steps": 20000,
  "learning_rate": 0.0001,
  "mining_opt_steps": 10,
  "energy_reg_weight": 0.1,
  "mining_noise_scale": 1.5,
  "use_langevin": false,
  "use_replay_buffer": false,
  "use_cd_loss": true,
  "use_residual_filter": false,
  "use_energy_schedule": false,
  "energy_loss_weight": 0.05,
  "adam_betas": [0.0, 0.999],
  "output_dir": "results/ds_inverse/q205_cd_adversarial",
  "notes": "Key ablation: CD loss (E_pos - E_neg) with adversarial opt_step negatives instead of Langevin. Diagnostic showed Langevin grad_norm=0.0018 (SNR=0.008) — chain is pure random walk because energy landscape has flat gradient wrt x. opt_step uses create_graph=True so model is trained to produce informative gradients. Tests whether CD loss itself is better than NCE when negatives are good (opt_step proven to work in q201 baseline). If this works but q202-q204 don't: Langevin is the broken piece, not CD. If this also stagnates: CD loss itself is the problem."
}
