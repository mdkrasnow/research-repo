# Ralph Loop Control

- `.claude/ralph/enabled` : presence enables the Stop-hook loop
- `.claude/ralph/config.json` : loop parameters (max iterations, batch size, etc.)
- `.claude/ralph/loop-state.json` : per-session counters (created by hook)

Use slash commands:
- `/ralph-on`
- `/ralph-off`
