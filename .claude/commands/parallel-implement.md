---
description: Parallelize independent TODO items within one project (max 5 subagents).
allowed-tools: Read, Write, Edit, Task, Bash(mkdir:*)
argument-hint: --project <slug>
---
# /parallel-implement
Use subagents to implement up to 5 independent TODO items, then consolidate back into todo/debugging/pipeline.
