# 2026-08-13

## WFB-EqM Stage 3 (fb_direct scalar-energy optimization track)

- All three Stage 3 300-step no-Adam arms COMPLETED. Fetched metrics to
  `projects/masked-EqM/results/wfb_stage3/`. Every arm WORSENS the deterministic
  held-out probe, monotone in alpha:
  - direct (alpha=0, job 38904393): 10.6244 -> 10.8324 (+0.208)
  - WFB (alpha=0.5, job 38903397): 10.6244 -> 11.2145 (+0.590)
  - FBGN (alpha=1 CG, job 38909657): 10.6245 -> 14.8811 (+4.257)
  All at accept_rate 1.000, zero skips of any kind.
- KEY: the raw-direct NEGATIVE CONTROL also goes the wrong way. So this is not
  an FBGN-specific defect — FBGN is the extreme point of a failure running
  through the whole alpha family, ordered exactly as step aggressiveness is.
- LM adaptive-damping smoke (job 38944527, 20 steps): rho only moved 1e-4 -> 2e-4
  and the probe still rose +1.99 in 20 steps; median R fell to 0.453 (vs 0.528
  fixed-rho). Evidence against a damping-only repair.
- Probe integrity AUDITED and PASSES: `build_pool` freezes (xt, t, y, ut) once at
  job start — images, labels, VAE latents, t, Gaussian eps, corrupted latent and
  target are all materialized once and reused byte-identically; model stays in
  `.eval()`. Confirmed empirically: two separate jobs report
  `probe_loss_initial` identical to 16 s.f. No probe repair needed.
  CAVEAT (now pinned in Stage 3A): the probe slice is
  `pool[max_steps : max_steps+8]`, so it MOVES when `--max-steps` changes —
  cross-run probe comparisons are only valid at equal `--max-steps`.
- RETRACTED the Stage 3 postmortem's central causal claim ("lambda_max growth
  caused the divergence"), for three reasons: (a) FBGN's probe damage is ~78%
  complete by step 25 and flat thereafter, while lambda_max grew for 275 more
  steps — timing decoupled; (b) damping was set as lambda = rho*lambda_max, so
  under M -> cM the operator A(A+lambda I)^-1 is INVARIANT by construction, i.e.
  uniform curvature growth cancels; (c) the lambda_max sequence and the late
  L_before spikes (40.7, 130.6) were measured on different fresh stochastic
  batches — heavy-tailed per-batch draws, not a parameter-space excursion. The
  postmortem's backbone-vs-head localization (1.53x vs 0.95x) is unaffected.
- Also established that the logged `rho_lm` (= LM reduction ratio, 0.991/0.911/
  0.528 by arm) CANNOT decide H1 vs H2: it is confounded with step length
  (median eta 3.8e-6 / 2.5e-3 / 1.19). Any smooth model looks accurate as
  eta -> 0.
- Wrote `documentation/wfb-eqm-stage3-audit-2026-08-13.md` (three-way table,
  probe audit, retraction, next action).
- Built Stage 3A frozen-checkpoint mechanism discriminator
  (`experiments/direct_energy/wfb_stage3a_mechanism_discriminator.py` +
  `slurm/jobs/wfb_stage3a_discriminator.sbatch` +
  `experiments/direct_energy/analyze_stage3a_discriminator.py`): no training, no
  optimizer, apply/evaluate/revert with bitwise restoration asserted. Scans eta
  at FIXED direction (separating step length from direction) and measures the
  eta-INDEPENDENT infinitesimal transfer d_V = grad L_V . p on an independent
  64-example trust bank. d_V >= 0 would decide H2 outright, since by first-order
  Taylor no step size and no damping can rescue an ascent direction.
- Gotcha found while validating: the FP64 CPU toy (`make_model`) is LINEAR in
  theta to machine precision at default EqM/DiT init (adaLN-zero gates + zeroed
  output projection), so the linearization-defect test sat at the 6.6e-16
  roundoff floor and measured nothing. `perturb(model, std=0.02)` is REQUIRED to
  give the toy genuine theta-curvature; after perturbing, the defect scales
  cleanly as O(eta^2) and D_B as O(eta). Encoded in
  `tests/test_stage3a_discriminator_metrics.py`.
