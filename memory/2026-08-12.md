# 2026-08-12

- WFB-EqM (Whitened Forward-Backward EqM) investigation started per detailed user spec:
  tests whether mixed input-parameter Jacobian whitening (`g_wfb = M^T(A+lambda I)^{-1/2}r`,
  `A = M M^T`, `lambda = rho*lambda_max(A)`) causally eliminates the fb_direct
  catastrophic-gradient instability. Staged Stage 0 -> Stage 5 with explicit gates.
- Found `projects/masked-EqM/CLAUDE.md` (project charter) covered only "Structured
  Start-State EqM" and had zero mention of the fb_direct thread despite it running since
  2026-08-01 -- user chose to update CLAUDE.md to document it as a second parallel track
  before proceeding (commit `d8f2841`).
- Stage 0 (operator correctness): implemented `mixed_gram_mv`/`estimate_lambda_max`/
  `lanczos_inv_sqrt_apply`/`compute_wfb_gradient` in `fb_direct/exact_hvp.py`, reusing
  existing `exact_field_vjp`/`field_jvp_direct` as the M^T v / M p primitives (no new
  Jacobian machinery needed). 21/21 FP64 CPU tests PASS (explicit-Jacobian toy test +
  synthetic extreme-singular-spectrum test, both against ground truth). Commit `da03875`.
- Stage 1 (zero-update real-checkpoint diagnostic): 3 submission attempts, 2 real bugs
  found+fixed:
  1. `pos_embed` (`nn.Parameter(requires_grad=False)`, fixed sinusoidal embedding) crashed
     `torch.autograd.grad` when passed the FULL param list -- `allow_unused=True` doesn't
     cover requires_grad=False inputs, only disconnected requires_grad=True ones. No prior
     diagnostic hit this since they always restricted to head/backbone subsets. Fixed
     commit `5fd6c05` + regression test.
  2. CPU/GPU device mismatch in the Lanczos tridiagonal eigendecomposition (`torch.eye`
     etc. defaulted to CPU, crashed against the GPU-resident Krylov basis) -- a GPU-only
     bug Stage 0's CPU-only tests couldn't have caught by construction. Fixed commit
     `27cf048`.
  3rd attempt (job 38484995) COMPLETED clean, 42min, gpu_test. **Result: raw spike/control
  g_raw_norm ratio 2.44x collapses to WFB g_wfb_norm ratio 0.95x** (statistically ~1) --
  confirmed independently on n=30 literal real clip-event batches (ratio 0.951), not just
  percentile selection. Backbone-dominant suppression (2.508->0.946) consistent with Stage
  A's redirect finding. Zero Lanczos breakdowns on 94 real batches; T_eigmax vs independent
  lambda_max agree to 0.27%. Caveat: g_wfb_norm not fully converged at k=8 vs k=12 (~7-12%
  further decrease) though T_eigmax converges by k=4 -- recommend k=12 for Stage 2.
  **Stage 1 gate PASSES all 5 criteria (D with a carried-forward caveat). Decision:
  PROCEED TO STAGE 2** (very short paired training test). Full report:
  `documentation/wfb-eqm-stage1-report-2026-08-12.md`.
