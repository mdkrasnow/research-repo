# 2026-08-06

- fb_direct Gate 0 (forward-backwards-direct decision tree, masked-EqM): job 37520759 FAILED on real epoch-40 checkpoint -- audit_mean_rel_error 1.524e-4/1.763e-4 vs 1e-4 threshold (cosine~1.0, loss_rel_diff~1e-5-1e-6, so NOT the CAFM-style mechanism-bug pattern -- direction/loss right, magnitude off). Root-caused to train.py's global `torch.backends.cuda.matmul.allow_tf32=True` (imported transitively by the gate0 script) truncating matmul mantissas across the real model's 12 SiT blocks in fb_direct/reverse_ops.py's manual VJPs; FP64 (7.6e-16) and FP32-CPU (3.9e-7, no tensor cores) unit tests on the tiny synthetic model never exercised TF32 so they stayed clean.
- One retune (per AGENTS.md decision-tree discipline): promoted `attention_reverse` (full softmax-backward matmul chain) and `layernorm_noaffine_reverse` (mean/inv-std reductions) in `projects/masked-EqM/fb_direct/reverse_ops.py` to strict FP32 accumulation via a local TF32-disabled context + `torch.promote_types(orig_dtype, float32)` upcast (floor not ceiling -- FP64 unit tests untouched, still pass at 7.6e-16/3.9e-7). Commit 4a6186c, pushed. All 17 fb-direct tests + test_scalar_ebm.py + test_energy_only.py still pass.
- Re-ran Gate 0 as job 37521466 (gpu_test, same checkpoint/2 batches) to evaluate the retune. pipeline.json: 37520759 moved to completed_runs (failed); results_variants.tsv created for masked-EqM logging both attempts.
