# 2026-07-21

- Set up repo-wide, git-tracked memory system at user's request, modeled on OpenClaw's
  `MEMORY.md` (curated, always-loaded) + `memory/YYYY-MM-DD.md` (daily append-only log)
  + promoted topic write-ups. Structure:
  - `MEMORY.md` — repo root, curated index.
  - `memory/topics/<slug>.md` — durable per-finding/topic notes, front matter
    `name`/`description`/`status`.
  - `memory/YYYY-MM-DD.md` — this file's pattern, one per day, running notes.
  - `memory/imports/` — reserved for memory pulled in from elsewhere.
  - Seeded `memory/topics/diff-eqm-v10-history.md` as a condensed, git-tracked copy of
    the diff-EqM v10 result (previously only in Claude Code's private per-session
    memory, which is not git-tracked and machine-local).
  - Updated `CLAUDE.md` to instruct checking `MEMORY.md` at session start and before
    non-trivial actions, alongside the existing AGENTS.md alignment-first protocol.
  - This is explicitly repo-wide (covers archived projects too), per user's explicit
    override of the AGENTS.md single-project (masked-EqM-only) scope for this
    infrastructure task.

- User pasted an external ChatGPT conversation proposing a scalar explicit-energy
  variant of EqM (train `E(x)` directly, derive the field as `∇E(x)`) plus a detailed
  evaluation plan (primary: unseen-corruption LPIPS generalization gap; secondary:
  seen-corruption LPIPS, FID/precision/recall, energy monotonicity during sampling,
  convergence efficiency, energy ranking AUROC). Promoted to
  `memory/topics/scalar-energy-eqm-proposal.md` since it's a candidate research
  direction, not just a one-off note. Idea is UNEVALUATED — not yet run through the
  project's mandatory variant-proposal / compatibility-check process, and no
  masked-EqM summer plan exists yet to gate it against.

- Implemented `ebm=scalar` mode in `projects/masked-EqM/` (models.py ScalarEnergyHead,
  forward refactor forcing grad-enabled autograd for all energy modes under sampler
  no_grad, full-channel CFG for scalar, CLI additions in train/sample_gd/eval_fid,
  ebm tag in experiment name). Field = -grad_x E, trained with unchanged transport MSE.
  CPU sanity tests all PASS (`tests/test_scalar_ebm.py`): shape/gradflow at zero init,
  finite-difference sign check, cfg conservativeness, no_grad sampling, transport smoke
  for none+scalar. Confirmed empirically: upstream `l2` mode has exactly-zero final-layer
  gradient at its zero init (degenerate stationary point) — scalar head does not.
  Untrained at any scale; needs variant-proposal write-up + CIFAR/paper gating before runs.
