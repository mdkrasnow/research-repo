# 2026-08-11

- Stage A (top-k high-gain SUBSPACE confirmation) infra built: `block_subspace_iteration_theta`
  (orthogonal iteration, validated vs explicit-Jacobian+SVD both on a real tiny model and a
  synthetic 13x-spectral-gap operator) + `topk_subspace_diagnostic.py` (tail-severity ladder
  2.5%/1%/0.5%/actual-clip, P_k/Q_k head-Jacobian projection metrics). See `documentation/topk_subspace_audit.md`.
- Multiple submission/infra failures before a clean run, all diagnosed and fixed:
  - Wrong sbatch env-var mechanism (needs `SBATCH_EXPORTS`, not shell-prefixed vars to `remote_submit.sh`).
  - Two bad-node incidents (CUDA-busy on holygpu8a31105; a genuine ~1h35m stuck-mount hang on
    holygpu8a29305 at the pool-build step, both excluded on resubmit).
  - Checkpoint path documentation was WRONG in `longitudinal_jacobian_audit.md`/`topk_subspace_audit.md`
    (assumed `/n/holylabs/.../masked-EqM/results/`; true root is
    `/n/netscratch/ydu_lab/Lab/mkrasnow_eqm/direct_energy_longer_retry/`) -- corrected in both docs.
  - Real bug: `energy_head.linear.bias` (ScalarEnergyHead) has an exactly-zero field-Jacobian
    column (summed BEFORE the token dim -> additive constant in z), causing
    `field_jvp_direct`/`_none` to raise on any head-only params list. Fixed with
    `allow_unused=True` + zero-substitution; added regression test (16/16 unit tests PASS).
  - Currently running as job 38312334 (excluding both bad nodes), 4th resubmission.
- Reconciled `pipeline.json` active_runs multiple times this session (jobs were stale "pending"
  while actually running/completed).
- Analyzed the two pre-registered causal single-lever tests (weight-decay, z-loss), both
  completed overnight:
  - **weight_decay=0.01: GATE FAILS.** weight_specnorm growth ~identical to zero-decay control
    (+0.96% vs +0.94% over 75k steps) -- generic AdamW drift, weight decay doesn't touch it.
  - **energy_zloss_lambda=1e-6: GATE PASSES, decisively.** E_std_mean -57.5% vs control's +19.5%
    over the same window -- opposite-signed trajectories. Strongest single-lever result on this
    thread to date. Candidate for a longer-horizon continuation measuring actual clip-rate impact
    (not yet directly measured -- this was an offline forward-only diagnostic).
  - Analysis jobs (38314263/38314272) both crashed on the known holylabs disk-quota `--out` write
    issue AFTER computing successfully; data recovered from tee'd stdout logs, not lost.
