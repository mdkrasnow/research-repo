# 2026-08-07

- fb_direct Test A (optimal-adjoint ceiling, Yilun decision tree): 4 failed attempts
  (37687761 frozen-tensor bug; 37688212 gpu_test MIG OOM; 37688514 OOM on full 80GB A100;
  37689193 gc-fix no effect). Isolated memory probe 37689738 confirmed genuine ~3.7GB/call
  leak in the hand-rolled create_graph=True JVP trick; replaced with official torch.func.jvp
  (commit 37ef495), probe 37691731 confirmed flat 5.93GB. Retry4 = job 37692043 COMPLETED.
- Test A v1 result INVALID by its own math: median rho_best=0.912 < rho_astar=0.954, but a*
  is feasible in the same least-squares problem so converged-argmin cosine >= rho_astar is a
  theorem — proves CGNR non-convergence (residuals only halved in 20 iters from zero start),
  NOT a ceiling. Canned "interpretation" string in the log also wrong for this branch.
- PI-level theory session (user asked for the theoretical gap in forward-backwards-direct):
  (1) semigradient drops dL/dC * dC/dtheta — O(1) term (Gate 1 cosine 0.62), no TD-style
  contraction rescues it; (2) deeper defect: the update field has non-symmetric Jacobian →
  not the gradient of ANY potential → no descent lemma; Gate 2's smooth non-exploding
  divergence is exactly non-conservative-flow behavior; (3) repair = exactness via
  Pearlmutter forward-over-reverse: dL/dtheta = d/dtheta[w^T grad_z E], w detached, computed
  as fwAD dual pass in z + ONE first-order backward over theta — exact same gradient as
  double-backward at ~2x forward cost and ordinary memory; (4) optional gradient penalty
  lambda*mean(||grad_z E||^2) folds into the SAME jvp direction by linearity (attacks the
  mixed-Hessian conditioning at source, which is what actually hurt ebm='direct').
- Implemented: fb_direct/exact_hvp.py (exact_fwrev_backward), train.py --exact-fwrev /
  --gp-lambda (single-rank guard: DDP allreduce never fires on this path), gate2_arm sbatch
  EXACT_FWREV/GP_LAMBDA envs, CGNR warm-start (x0=a_star) + per-iter rho tracking +
  best-iterate selection in adjoint_optimization.py + corrected Test A driver.
- TWO REAL torch 2.7.1 fwAD bugs found+worked around in exact_hvp (_forward_ad_safe_ops):
  (a) softmax AND logsumexp forward-AD formulas do an in-place mutation that breaks
  forward-over-reverse ("output 0 of ExpBackward0 ... version 1"); manual
  exp(x-amax.detach())/sum composition is clean. (b) native_layer_norm's fwAD formula treats
  saved mean/rstd as CONSTANTS — tangent VALUE exact but grad_theta(tangent) silently wrong
  (5.7e0 vs 1.3e-15 in microtest); manual mean/var composition fixes. Silent-wrong-answer
  class, landed exactly on adaLN LN sites. Also: fused SDPA math composite in-place issue →
  force timm manual attention path during both passes.
- tests/test_fb_direct_exact_hvp.py: fwrev == double-backward at FP64 machine precision
  (130 param tensors, gp=0 and gp=0.05, label-dropout determinism, ebm guard). All existing
  suites still pass (test_forward_backwards_direct, scalar_ebm, energy_only, cache_adjoint,
  adjoint_optimization incl new warm-start test).
- Test A v2 (job 37711914, 8min): VALID. median rho_astar=0.958, rho_best=0.974 (best-iter
  0.979), gain +0.017, 16/20 batches improved. Optimal linear cache-adjoint ceiling ~0.97-0.98
  (lower bound). Verdict: corrector route offers marginal headroom (Gate 2 showed 0.62
  diverges; even 0.97 reconstruction of the omitted term is uncertain to train stably) —
  closed in favor of exact-fwrev. Arms 37711939 (λ0) / 37711945 (gp .05) still queued.
- BOTH fwrev arms landed clean, fb_direct thread CLOSED: lambda0 (37711939) median loss
  10.700 squarely in exact-direct control band 10.5-11.1, 0 clips — in-vivo exactness
  validated; GP arm (37711945) stable, loss_main 10.76 (unaffected), mean||grad_z E||^2
  down 9% (12.69->11.52), grad norms slightly smaller. Semigradient formally superseded by
  exact-fwrev. Decision to user/PI: FID-scale test of exact-fwrev direct (+/-GP) vs settling
  on none/dot backbone. PI draft appended to pi-updates.md; needs_user_input set.
