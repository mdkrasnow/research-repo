# 2026-08-10

- fwrev epoch40->80 arms (37780076 lambda0, 37780078 gp005): quartile forensics revealed both
  arms reproduce direct's growing-clip-rate instability signature (0.13-0.15% -> 0.28-0.31%
  clip rate over the run so far), NOT a double-backward computation artifact since exact-fwrev
  is mathematically identical to double-backward. Key finding: loss_gp's RELATIVE growth rate
  is IDENTICAL between lambda0/GP arms (~+0.40%/+0.41%) -- GP only rescales the level by a
  constant ~9%, doesn't touch the slope. Theoretical read: E[||grad_z E||^2] is a MEAN-based
  penalty and cannot control a TAIL statistic (clip events driven by rare large local
  curvature) -- Markov/Chebyshev-type mismatch. field/target norm shrinkage (~4.8%, matches
  1/(1+lambda) exactly) confirmed stable across all quartiles in both arms, not drifting.
- Proposed fix ranked by rigor: (1) power-iteration penalty on ||grad_z^2 E||_op (spectral
  norm/Lipschitz constant of the field) -- reuses exact_hvp.py's forward-over-reverse machinery
  almost for free; (2) spectral normalization of backbone weights (hard Lipschitz bound by
  construction, Du & Mordatch 2019 IGEBM precedent -- Yilun's own established EBM-stability
  recipe; the exact tensor set is already enumerated as fb_direct's `reverse_active` set);
  (3) adaptive/quantile gradient clipping (symptom-layer, decouples "threshold outgrown" from
  "landscape actually rougher").
- Built + FP64-verified (before any GPU time, per project discipline) the offline mechanism
  check: fb_direct/exact_hvp.py gains hvp_z (z-space Hessian-vector product, double-backward,
  offline-only exemption matching Test A precedent) and power_iteration_spectral_norm
  (per-sample, sign-agnostic |Rayleigh quotient| tracking -- verified against exact eigh()
  including a deliberately indefinite-Hessian/negative-dominant-eigenvalue case,
  tests/test_fb_direct_curvature_probe.py, all pass).
- experiments/direct_energy/curvature_clip_diagnostic.py: offline probe on real checkpoints,
  fixed batch bank shared across checkpoints, tests (A) does spectral_norm actually grow over
  training per arm, (B) pooled Pearson/Spearman: does spectral_norm predict theta-space
  grad_norm better than loss_gp does. Job 37947153 submitted, then CANCELLED (operator error:
  a checkpoint prune concurrent with the submission deleted 3/5 of its input checkpoints before
  checking active_runs for dependents -- caught before any checkpoint load via log inspection,
  zero wasted compute; see feedback memory feedback_checkpoint_prune_vs_inflight_jobs).
  Resubmitted as v2, job 37947377, with the surviving checkpoint set (1600000/1925000/2050000/
  2175000/2450000, both arms).
- SLURM dependency chaining added: direct_energy_ddp_continue.sbatch now supports
  CKPT=AUTOLATEST:<dir> resolved at job START (not submission) time, for exactly this class of
  race -- avoids hardcoding a checkpoint path before it exists. remote_submit.sh gained
  SBATCH_DEPENDENCY passthrough. Resume jobs 37946478/37946483 queued (--dependency=afterany)
  to auto-fire when 37780076/37780078 hit the 48h wall, no manual intervention needed.
- Also learned: SBATCH_EXPORTS values flow through TWO shell layers (local ssh command-string
  construction, then remote bash) -- `|` as a path separator got reinterpreted as an actual
  shell pipe, silently breaking `sbatch --export` into a broken pipeline (confusing
  "Permission denied"/"No such file" errors that were actually failed pipeline stages, not
  filesystem issues). Fixed with a literal alphanumeric token (`ZPATHZ`) instead of any
  shell-metacharacter separator.

- Curvature diagnostic (after 3 infra failures -- bad node holygpu8a31105 excluded, SDPA-backend
  bug fixed) COMPLETED (job 37950953). NEGATIVE RESULT: spectral_norm(grad_z^2 E) does NOT
  predict theta-space grad_norm better than GP's order-1 quantity mean(grad_z E)^2 -- max
  aggregation (theoretically strongest candidate) essentially zero correlation (pearson 0.007,
  spearman -0.008). Growth trend flat/noisy for both arms (inconclusive, possibly underpowered
  for a tail effect at only 20 batches/checkpoint). CONCLUSION: do not build the spectral-norm/
  curvature-penalty training path -- not supported. Growing clip-rate mechanism remains
  unexplained by z-space quantities; if pursued, next hypothesis should target theta-space
  (weight-norm growth, theta-Hessian) not input-boundary energy curvature. Good example of
  mechanism-check-before-code saving real compute (~2 GPU-days) on an unsupported theory.

- activation_scale_diagnostic.py (job 38066731, forward-only, gpu_test): GPT-lineage/scaling
  follow-up. Median QKV/proj weight spectral norm grows +25.6% monotonically over ~1.2M steps,
  IDENTICAL between lambda0/GP arms (GP touches mean(grad_z E^2), never raw weight scale).
  Attention-logit scale mean flat (~2.1-2.25), max noisy (57-63, not monotonic) -- QK-norm
  deprioritized, weak signal. Raw |E| grows +25-42% (E_abs_max), also arm-identical.
  External review caught two errors before acting on this: (1) causal overclaim -- correlation
  across two arms sharing every other cause isn't proof; (2) |E| is gauge-ambiguous
  (E -> E+C(theta) doesn't change field f=-grad_z E; ScalarEnergyHead.linear has a free bias
  term), so E_abs_max growth may be an inert offset drift, not a real field-scale signal.
  E_std_mean (gauge-safe) is weaker/non-monotonic (3939->4818->4226).
- field_sensitivity_diagnostic.py (job 38074051, gpu_test, after 2 infra retries -- CPU/CUDA
  device bug in the perturbation direction, then attn_logit_stats hardcoded energy_only=True
  breaking on ebm='none'): built to test the corrected hypothesis with (a) the missing `none`
  control and (b) a gauge-safe function-space sensitivity U_t=||f(theta+dtheta,x)-f(theta,x)||
  /||dtheta|| via finite-difference random parameter perturbations (5 dirs x 3 batches,
  eps=1e-3 relative per-tensor). BOTH corrections came back NEGATIVE for the leading hypothesis:
  (a) none's weight-specnorm also grows, same order of magnitude per-step as direct
  (+1.65%/100k steps none vs +2.1%/100k direct) -- generic AdamW-zero-weight-decay Transformer
  drift, not direct-specific. (b) isotropic U_t DECREASES over training for direct (0.510->
  0.469->0.446, -12.4%) while none's mildly increases (0.452->0.455->0.464, +2.7%) -- opposite
  of the amplification hypothesis's prediction. Caveat: U_t as measured is a MEAN/isotropic
  quantity (random Gaussian directions) and catastrophic clip events are a TAIL phenomenon
  (0.13-0.5% of batches) -- same structural blind spot that killed GP and the z-Hessian
  curvature test. Random directions won't align with whatever rare direction a real clip event
  hits. Next higher-power test if pursued: replay actual logged catastrophic-clip batches
  (e.g. lambda0 step 2811423, grad_norm=80839) through matched direct/none checkpoints,
  measure sensitivity along the REAL gradient direction at that event, not a random one.
- Two causal single-lever tests still queued (H200-constrained, ~10-12h estimated start):
  38070968 (--weight-decay 0.01, new train.py flag, was hardcoded 0) and 38070989
  (--energy-zloss-lambda 1e-6, new flag: mean(E^2) regularizer via a separate light forward +
  ordinary backward accumulated into exact_fwrev_backward's .grad tensors), both continuing
  fwrev lambda0 from ckpt 2825000.pt (job 37780076), MAX_STEPS=100000, vs the zero-decay
  zero-zloss resume (37946478/83) as control. These remain informative regardless of the
  correlational field-sensitivity result since they're direct perturb-and-observe experiments.
- Running tally: GP (dead, tail-blind), z-Hessian curvature (dead, wrong operator/null
  correlation), weight/logit growth (generic, not direct-specific), isotropic field-sensitivity
  (wrong-signed trend) -- four diagnostics down, mechanism for direct's growing-clip-rate
  instability still unexplained. All four share a common thread: MEAN-based or ISOTROPIC
  measurements can't see a TAIL-driven phenomenon. Any future diagnostic on this thread should
  target the actual observed outlier batches/directions, not aggregate/average statistics.

- spike_batch_amplification_diagnostic (job 38105538) COMPLETED. Spike/ordinary gain ratio
  ~3.4x direct vs ~2.9x none (similar order), same early-layer localization pattern
  (x_embedder/block0/block1) in both arms -- does NOT find a direct-specific tail-amplification
  mechanism at this (mild, top-2%-of-500) tail depth. Flagged scope caveat: this "spike" band is
  far milder than the true catastrophic tail (~1e-4-1e-5 batch probability).
- NCCL infra failure this session (unrelated to the above): resume jobs 37946478 (lambda0) /
  37946483 (GP) both SIGABRT'd on a scalar-value ALLREDUCE 30-min watchdog timeout at ~step
  2.9M, same wall-clock window on different nodes -- shared network/filesystem stall, not a
  per-node fault, not a model/training-logic issue (loss values normal right up to the hang).
  Cancelled the now-doomed dependent FID-eval jobs (38044076/38044090, would have failed against
  a nonexistent epoch80.pt), resubmitted resume2 (38103331 lambda0, 38103380 GP, both
  CKPT=AUTOLATEST) + re-chained FID evals (38103466, 38103515). All PENDING/Priority as of
  session pause.
- User directive (2026-08-10, post spike-batch result): stop generic stabilization candidates,
  build a PROPER matched-batch-replay diagnostic. Phase 0 audit of the just-completed spike
  diagnostic found the likely root cause of its uninterpretable ~4-order-of-magnitude direct/none
  gain gap: direct's reported "w_norm" (exact_fwrev_backward's internal JVP direction) equals
  -(2/(B*D)) * canonical_residual, i.e. it's the SAME residual r=field-target used by none's gain,
  just rescaled by the training loss's 1/(B*D) mean-reduction constant (~1e-4 to 1e-5 for this
  model). This alone brings the previously reported gains to the same order of magnitude once
  corrected -- confirmed both algebraically and at FP64 machine precision (new test
  test_field_vjp_direct_sign_relation_to_fwrev_w). Built exact_field_vjp/field_vjp_none
  (fb_direct/exact_hvp.py) -- arbitrary-direction VJP of the field w.r.t. theta, decoupled from
  any loss reduction convention -- and matched_replay_jacobian_diagnostic.py implementing the
  full 2x2 matched-replay design (direct-selected + none-selected spike/control batches, each
  replayed through BOTH checkpoints) with canonical residual validation, backbone/head/global/
  per-layer breakdown, and batch-level bootstrap CI for the cross-model interaction Delta.
  Directly targets Hypothesis D (selection artifact) which the OLD diagnostic could not rule out
  since it never cross-replayed. 8/8 new+existing unit tests PASS at FP64 CPU (double-backward
  reference, torch.autograd.grad reference, AND independent finite-difference ground truth) --
  committed 5264004, pushed. BLOCKED on submission: cluster SSH ControlMaster needs interactive
  2FA re-auth (scripts/cluster/ssh_bootstrap.sh) to fetch exact checkpoint paths
  (direct=...2825000.pt, none=...epoch80.pt per the prior spike-diagnostic's pipeline.json
  entry, dir prefixes not yet recovered) and launch the sbatch job.

- matched_replay_jacobian_diagnostic (job 38127797) COMPLETED in 13min (much faster than the
  2-4h estimate). CLEAN NULL RESULT for Hypothesis A -- first decisive result after 5 prior
  null/inconclusive diagnostics this session (GP, z-Hessian curvature, weight/logit growth,
  isotropic field-sensitivity, spike-batch-amplification-uncorrected). R_direct=2.58 vs
  R_none=2.49 (spike/control A_backbone ratio, own-selection, matched 2x2 replay design),
  Delta=0.034 log-units, 95% bootstrap CI [-0.119, 0.279] comfortably includes 0 --
  statistically indistinguishable. Since A=||J^T(r/||r||)|| is residual-magnitude-normalized BY
  CONSTRUCTION, this rules out Hypothesis C directly and supports Hypothesis B: shared
  high-sensitivity batches / a generic Transformer-Jacobian-conditioning property of certain
  images-timesteps, NOT a direct-EBM-parameterization-specific pathology. Secondary (non-primary)
  asymmetry: cross-selection ratios weaker (direct on none's spikes=1.85, none on direct's
  spikes=1.22) -- grad_norm-based spike selection has a partial model-specific component even
  though own-selection magnitudes match. Layer localization mixed/inconsistent across blocks, no
  clean single-layer story. Table 1 canonical-residual identity validated to ~2e-7 relative error.
  CONCLUSION: deprioritize further tail-amplification probes; the two causal single-lever tests
  (38070968 weight-decay, 38070989 z-loss, both still PENDING/seas_gpu) and the FID-scale
  evaluation (resume2 chain 38103331/38103380 -> 38103466/38103515) are now the deciding evidence.
- Bookkeeping: found + fixed a pre-compaction gap where spike_batch_amplification_diagnostic's
  successful completion (job 38105538) was never recorded in pipeline.json, and its superseded
  earlier submission (38093245) was left stale in active_runs. Backfilled both into
  completed_runs. Lesson: always verify active_runs/completed_runs state explicitly after a
  context compaction, don't assume prior-session bookkeeping completed just because the
  conversation summary describes a job as done.
- Running tally now SIX diagnostics down (GP, z-Hessian curvature, weight/logit growth, isotropic
  field-sensitivity, spike-batch [normalization-flawed], matched-replay Jacobian amplification
  [corrected, clean null]) -- residual-conditioned Jacobian amplification, at any tail depth
  probed so far (top 2-2.5% of ~500-960 batches), does not explain direct's growing-clip-rate
  instability. The mechanism remains open at the TRUE catastrophic tail (~1e-4-1e-5 probability,
  never directly probed -- every diagnostic so far has been a much milder "spike" proxy).

- CORRECTION (external review) to the "clean null" entry above: the matched_replay_jacobian_
  diagnostic's interaction statistic (Delta=0.034, CI crossing zero) compared each model on its
  OWN independently-selected spike batches, not the matched same-batch cross-model contrast the
  design was built to test -- exactly the selection artifact (Hyp D) the experiment existed to
  catch. Corrected statistic (job 38136938, none held FIXED at late ref, K=24, early/mid/late
  direct sweep): late interaction +0.456 (matches user's independent hand-derivation +0.452 from
  the SAME raw numbers -- real consistency check), but all 3 stages' CIs crossed zero (n=24
  underpowered), non-monotonic trend (early +0.209, mid -0.009, late +0.456).
- FOLLOW-UP (job 38150502, seas_gpu, 57m, progress-matched + K=64 + new sigma_1/alignment
  diagnostic): Phase 0 audit found `none`'s training is a chained-resume lineage (longer40/60/80)
  with checkpoints at 0%/1.15%/0.885% step mismatch vs direct's early/mid/late -- the prior
  "none has no early checkpoint" premise was an infra-discovery gap, not real. With progress-
  matched none + K=64: I(late)_A-based = +0.321, CI [0.008, 0.654] -- EXCLUDES ZERO, first
  significant single-checkpoint result in this whole investigation. I(late)-I(mid)=+0.421 CI
  [0.069,0.769] also excludes zero. G=I(late)-I(early)=+0.305 but CI [-0.066,0.688] still
  crosses zero (conservative combination). grad-based late interaction (+0.220, CI
  [-0.102,0.685]) does NOT exclude zero -- A-based and grad-based disagree at late stage, so
  treat as suggestive not airtight. residual_ratio~1.0 again at K=64 (Hyp C still rejected).
  NEW: Table 7 tail quantiles show direct_own_spike has tail_anisotropy_proxy (p99/p50) = 7.73
  vs direct_own_control=1.16, while none_own_spike=2.34/none_own_control=1.15 -- direct's spike
  population has a much heavier upper tail than none's despite similar MEDIAN spike/control
  ratios (~2.5 both) -- the effect is tail-specific, not a median shift. NEW mechanism probe
  (matrix-free sigma_1(J_backbone) + residual/top-singular-vector alignment via
  power_iteration_theta_sigma1, fb_direct/exact_hvp.py, validated vs explicit-Jacobian+SVD on
  toy model): on direct-selected late-stage batches, direct's alignment_ratio(spike/control)
  =6.62 vs none's=2.92 (>2x more direct-specific), while sigma_1_ratio=1.40 vs none's=1.21
  (both models' conditioning worsens somewhat, direct only modestly more). Points to worse
  RESIDUAL ALIGNMENT with existing high-gain directions as the primary direct-specific
  mechanism, not worse Jacobian conditioning per se (conditioning growth is shared/modest
  across both arms). See documentation/longitudinal_jacobian_audit.md,
  experiments/direct_energy/longitudinal_jacobian_diagnostic.py, pipeline.json completed_runs
  entry "longitudinal_jacobian_diagnostic_progress_matched" for full numbers.
